# LiteLLM 智能路由代理平台 — 项目文档

> **文档版本**: 2.0  
> **最后更新**: 2026-08-07  
> **项目路径**: `/Users/orange/litellm-install`  
> **适用对象**: 新接手项目的开发/运维人员  
> **变更说明**: v2.0 新增核心模块代码级实现注释、关键技术决策说明、外部技术文档参考

---

## 目录

1. [项目概述](#1-项目概述)
2. [系统架构](#2-系统架构)
3. [核心功能清单](#3-核心功能清单)
4. [项目文件结构](#4-项目文件结构)
5. [Docker 部署架构](#5-docker-部署架构)
6. [配置文件详解](#6-配置文件详解)
7. [核心模块实现原理](#7-核心模块实现原理)
8. [Tauri 桌面应用](#8-tauri-桌面应用)
9. [网页端管理界面](#9-网页端管理界面)
10. [CLI 命令行工具](#10-cli-命令行工具)
11. [部署与安装指南](#11-部署与安装指南)
12. [运维与监控](#12-运维与监控)
13. [常见问题与排障](#13-常见问题与排障)
14. [开发约定与注意事项](#14-开发约定与注意事项)
15. [技术参考与外部文档](#15-技术参考与外部文档)

---

## 1. 项目概述

### 1.1 项目定位

本项目是一个基于 **LiteLLM** 的智能 LLM 路由代理平台，实现了多模型管理、智能任务路由、语义安全检测、成本控制和可视化管理的完整解决方案。

### 1.2 核心价值

| 能力 | 说明 |
|------|------|
| **智能路由** | 根据用户问题类型自动选择最合适的模型，简单问答用本地模型（零成本），复杂推理用高端模型 |
| **多模型管理** | 统一管理阿里云百炼、OpenAI、Anthropic、Ollama 等多个供应商的模型 |
| **成本控制** | 按用户/密钥维度设置预算上限，自动追踪花费，防止超支 |
| **安全检测** | PII 脱敏、越狱攻击拦截、MMLU 14 域分类，保护 LLM 调用安全 |
| **复杂任务编排** | 自动检测复杂任务 → 分解为子任务 → 分配最优模型 → 汇总结果 |
| **语义缓存** | 基于向量相似度匹配相似请求，命中缓存跳过 API 调用，节省成本 |
| **可视化管理** | 桌面 App + 网页端双重管理界面，图形化操作所有功能 |

### 1.3 技术栈

- **LLM 代理**: LiteLLM v1.82.3（控制面 + 数据面分离架构）
- **向量缓存**: Qdrant v1.16.3（语义缓存向量数据库）
- **数据库**: PostgreSQL 16.4（模型/密钥/花费记录）
- **缓存/限流**: Redis 7（路由状态 + 限流计数器）
- **监控**: Prometheus v3.8.1 + Grafana 11.5.2
- **聊天前端**: Open WebUI v0.9.2
- **安全代理**: Semantic Router（Flask，自定义实现）
- **编排引擎**: Python 自研任务编排器
- **桌面应用**: Tauri v2（Rust 后端 + HTML/JS 前端）
- **本地模型**: Ollama + qwen2.5:latest（零成本兜底层）

---

## 2. 系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                        用户访问层                                    │
├──────────────┬──────────────┬──────────────┬────────────────────────┤
│  Tauri App   │  Open WebUI  │  网页管理端   │    CLI 命令行          │
│  (桌面应用)   │  (聊天界面)   │  (localhost   │   (litellm_cli.py)    │
│  5个页面      │  (:3001)     │   :3002)     │                       │
└──────┬───────┴──────┬───────┴──────┬───────┴───────────┬────────────┘
       │              │              │                    │
       ▼              ▼              ▼                    ▼
┌──────────────────────────────────────────────────────────────────────┐
│                    Semantic Router (:8888)                           │
│            安全检测 + MMLU 14 域分类代理                              │
│  ┌─────────┐  ┌──────────┐  ┌────────────┐  ┌──────────────────┐   │
│  │PII 检测 │  │越狱检测   │  │域分类       │  │幻觉检测(可选)     │   │
│  │7种类型  │  │5种攻击   │  │14个MMLU类  │  │LLM-based         │   │
│  │mask/block│  │block     │  │keyword+LLM│  │header/body/block │   │
│  └─────────┘  └──────────┘  └────────────┘  └──────────────────┘   │
└──────────────────────────┬───────────────────────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────────────┐
│                LiteLLM Worker (:4001) — 数据面                       │
│                                                                        │
│  ┌──────────────┐  ┌──────────────┐  ┌───────────────────────────┐  │
│  │ 智能任务路由  │  │ 语义缓存      │  │ 配额追踪                   │  │
│  │ task_router  │  │ Qdrant       │  │ quota_tracker             │  │
│  │ 回调         │  │ similarity   │  │ 回调                      │  │
│  │              │  │ ≥0.95        │  │                           │  │
│  │ 规则→LLM分类 │  │ TTL=24h      │  │ Key/User 维度             │  │
│  └──────┬───────┘  └──────────────┘  └───────────────────────────┘  │
│         │                                                              │
│  ┌──────▼────────────────────────────────────────────────────────┐  │
│  │              LiteLLM Router (usage-based-routing-v2)          │  │
│  │   fallbacks: qwen3.6-plus → qwen-plus → ... → qwen2.5-local  │  │
│  └──────┬────────────────────────────────────────────────────────┘  │
└─────────┼────────────────────────────────────────────────────────────┘
          │
          ▼
┌──────────────────────────────────────────────────────────────────────┐
│                      模型供应商层                                      │
├────────────────┬────────────────┬────────────────┬──────────────────┤
│  阿里云百炼     │  OpenAI        │  Anthropic     │  Ollama (本地)   │
│  qwen-plus     │  gpt-4o        │  claude-3.5    │  qwen2.5:latest  │
│  qwen3.6-flash │  (直连+OR)     │  sonnet        │  (零成本兜底)     │
│  qwen3.6-plus  │                │                │                  │
│  qwen3-max     │                │                │                  │
│  deepseek-v3   │                │                │                  │
└────────────────┴────────────────┴────────────────┴──────────────────┘

┌──────────────────────────────────────────────────────────────────────┐
│                    支撑服务层                                          │
├──────────────┬──────────────┬──────────────┬────────────────────────┤
│ PostgreSQL   │ Redis 7      │ Qdrant       │ Prometheus + Grafana   │
│ (:5432)      │ (:6379)      │ (:6333)      │ (:9090)    (:3000)     │
│ 模型/密钥/   │ 路由状态/     │ 语义缓存     │ 指标采集 + 可视化       │
│ 花费记录     │ 限流计数器    │ 向量存储     │                        │
└──────────────┴──────────────┴──────────────┴────────────────────────┘

┌──────────────────────────────────────────────────────────────────────┐
│                LiteLLM Admin (:4000) — 控制面                        │
│                管理 UI + 配置 API + 密钥管理                           │
└──────────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────────┐
│                Orchestrator Web (:3002) — 编排平台                    │
│  仪表盘 + 智能对话(SSE) + 配置向导 + 模型管理 + .env 读写 API         │
└──────────────────────────────────────────────────────────────────────┘
```

### 2.2 请求流转路径

**标准请求路径（Open WebUI / 外部客户端）**:
```
用户 → Open WebUI (:3001) → Semantic Router (:8888) → LiteLLM Worker (:4001) → 模型供应商
                                ↑                          ↑
                          PII/越狱/域分类            task_router 智能路由
                          (添加 SR 头)              (读取 SR 头增强路由)
```

**编排请求路径（Tauri App / 网页管理端）**:
```
用户 → Tauri App / 网页 (:3002) → Semantic Router /check (:8888)  [安全预检]
                                     ↓
                                 Orchestrator 编排引擎
                                     ↓
                              ┌──────────────────┐
                              │ 1. 复杂度检测      │
                              │ 2. 任务分解(auto) │
                              │ 3. 成本规划       │
                              │ 4. 子任务执行     │ ← 直接调用 Worker (:4001)
                              │ 5. 结果汇总       │
                              └──────────────────┘
```

### 2.3 控制面与数据面分离

| 维度 | Admin (控制面) | Worker (数据面) |
|------|----------------|-----------------|
| **端口** | 4000 | 4001 |
| **配置文件** | config_admin.yaml | config_worker.yaml |
| **职责** | 用户管理、Key 生成、配置变更、Dashboard | LLM 请求处理、路由、缓存、负载均衡 |
| **回调** | prometheus | prometheus + task_router + quota_tracker |
| **模型列表** | 仅 bailian-embedding | 全部模型 + auto 虚拟模型 |
| **共享** | PostgreSQL + Redis（通过 DATABASE_URL 和 REDIS_URL） |

---

## 3. 核心功能清单

### 3.1 智能任务路由 (Phase 2)

- **触发方式**: 请求中 `model="auto"` 或 `model="auto-route"`
- **分类策略**: 两层混合分类
  - 第一层: 正则规则匹配（零成本、零延迟）
  - 第二层: LLM 意图识别（低成本、低延迟兜底）
- **分类器自动选择**:
  - `DASHSCOPE_API_KEY` 已设置 → qwen3.6-flash（云端，10s 超时）
  - `DASHSCOPE_API_KEY` 为空 → qwen2.5:latest（Ollama 本地兜底）
- **任务类型映射**:

| 任务类型 | 推荐模型 | 说明 |
|----------|----------|------|
| simple_qa | qwen2.5-local | Ollama 本地，零 API 费用 |
| general | qwen-plus | 百炼通用性价比 |
| coding | deepseek-v3 | DeepSeek 擅长编程 |
| math_logic | deepseek-v3 | DeepSeek 擅长推理 |
| complex_reasoning | qwen3.6-plus | 百炼高质量综合推理 |

- **推理模型自动流式**: qwen3.6-flash/plus、qwen3-max、deepseek-v3 生成 reasoning tokens 耗时长，自动启用 `stream=true` 避免 HTTP 超时
- **SR 域分类增强**: 读取 Semantic Router 的 `X-SR-Domain` 头，STEM 域 → 数学/逻辑模型，CS/工程 → 编程模型

### 3.2 配额追踪与预算管理 (Phase 2)

- **预算默认值**: 新密钥 max_budget=$10/30d
- **安全上限**: max_budget=$1000/365d
- **追踪维度**: Virtual Key + User ID + Model
- **Prometheus 指标**:
  - `litellm_quota_key_spend_total` — 按 Key 累计花费
  - `litellm_quota_user_spend_total` — 按 User 累计花费
  - `litellm_quota_key_requests_total` — 按 Key 累计请求数
  - `litellm_quota_key_spend_by_model` — 按 Key+Model 累计花费
- **注意**: 分类请求（task_router 内部调用）通过 `metadata._task_router_classify=True` 标记，不计入配额

### 3.3 语义缓存 (Phase 3)

> **技术原理**: LiteLLM 的 Qdrant 语义缓存通过向量相似度匹配历史 LLM 响应，而非精确键匹配。请求到达时，将输入文本通过 embedding 模型转为向量，在 Qdrant 集合中搜索相似向量；若相似度超过阈值则返回缓存响应（cache hit），否则正常调用 LLM 并存入 Qdrant（cache miss）[$TRAE_REF](https://docs.litellm.ai/docs/proxy/caching)。Qdrant 使用 Cosine 余弦相似度，向量插入时归一化，搜索时等同于点积运算 [$TRAE_REF](https://qdrant.tech/documentation/concepts/search/)。

- **技术**: Qdrant 向量数据库 + DashScope text-embedding-v3
- **相似度阈值**: 0.95（低于此值会误命中）
- **TTL**: 86400 秒（24 小时自动过期）
- **向量维度**: 1024（DashScope text-embedding-v3 默认）
- **量化**: binary（节省存储空间）
- **依赖**: `DASHSCOPE_API_KEY` 必须设置，否则 embedding 调用 401 阻断主请求
- **选择性缓存**: 编排引擎对 simple_qa/general 类型启用缓存，复杂子任务禁用缓存

> **Binary 量化**: 将每个向量分量压缩为 1 个 bit，实现 32 倍压缩。对于 1024 维向量配合 rescoring 可达到高召回率 [$TRAE_REF](https://qdrant.tech/documentation/manage-data/quantization/)。

### 3.4 复杂任务编排 (Phase 4)

- **复杂度检测**: 多步骤关键词、请求长度>100字、句子数>2
- **任务分解**: LLM 将复杂任务拆分为 2-5 个子任务，标注依赖关系
- **成本规划**: 为每个子任务选择成本最优模型并估算费用
- **按序执行**: 按依赖顺序执行子任务，前置结果作为上下文传入
- **结果汇总**: LLM 将多个子任务结果汇总为连贯回答
- **对话上下文**: 支持传入对话历史（最近 10 轮），确保多轮对话连贯

### 3.5 Semantic Router 安全代理 (Phase 5)

- **架构**: Flask 代理服务，位于 LiteLLM Worker 前方
- **插件链**: PII 检测 → 越狱检测 → 域分类 → (幻觉检测)

#### PII 检测（7 种类型）

| 类型 | 正则策略 | 动作 |
|------|----------|------|
| EMAIL_ADDRESS | 邮箱格式 | mask → [EMAIL] |
| PHONE_NUMBER | 中国手机号 + 美国格式 | mask → [PHONE] |
| US_SSN | XXX-XX-XXXX | mask → [SSN] |
| CREDIT_CARD | 13-16 位数字 | mask → [CARD] |
| IP_ADDRESS | IPv4 标准格式 | mask → [IP] |
| ID_CARD | 中国身份证 18 位 | mask → [ID] |
| BANK_ACCOUNT | IBAN 格式 | mask → [BANK] |

> **注意**: PII 正则使用 `(?<!\d)` 和 `(?!\d)` lookbehind/lookahead 断言，而非 `\b`，因为 `\b` 不支持中文字符边界。

#### 越狱检测（5 种攻击类型）

| 攻击类型 | 检测模式 | 动作 |
|----------|----------|------|
| DAN_ATTACK | "you are now DAN" | block |
| INSTRUCTION_OVERRIDE | "ignore previous instructions" | block |
| ROLE_MANIPULATION | "pretend you are unrestricted" | block |
| SAFETY_BYPASS | "bypass safety restrictions" | block |
| PROMPT_INJECTION | "reveal system prompt" | block |

#### 域分类（MMLU 14 类）

- **类别**: math, physics, chemistry, biology, computer_science, engineering, business, law, economics, health, psychology, philosophy, history, other
- **分类策略**: 关键词预筛（零成本）→ LLM 兜底
- **SR 响应头**: `X-SR-Domain`, `X-SR-PII-Types`, `X-SR-Jailbreak-Types`
- **路由增强**: custom_callbacks.py 读取 `X-SR-Domain` 头，STEM 域 → math_logic 模型，CS/工程 → coding 模型

### 3.6 Tauri 桌面应用 (Phase 5)

- **5 页面结构**: 总览 / 用量 / 对话 / 模型管理 / 设置
- **Docker 服务管理**: 启动/停止/重启，实时健康状态
- **.env 配置编辑**: 图形化编辑 API Key，支持单字段保存和批量保存
- **智能重启**: 根据变更的配置键自动映射到受影响的 Docker 服务，仅重启必要服务
- **对话历史**: JSON 文件持久化存储，支持多对话存档、自动标题生成
- **模型管理**: 可视化添加/删除模型，替代 CLI 交互命令
- **SSE 代理**: Tauri webview 无法直接连接 http://localhost:3002（混合内容拦截），通过 Rust 后端 `chat_request` 命令代理 SSE 请求

### 3.7 三层 Fallback 容灾

> **技术原理**: LiteLLM 的 Fallback 机制在调用耗尽重试次数后仍失败时，按顺序回退到备用模型组。与 `usage-based-routing-v2` 路由策略配合，该策略过滤已超出 TPM/RPM 限制的部署，路由到当前使用量最低的部署 [$TRAE_REF](https://docs.litellm.ai/docs/proxy/reliability)[$TRAE_REF](https://docs.litellm.ai/docs/routing)。

```
云端高质量 → 云端降级 → 本地兜底（qwen2.5-local，零成本、零依赖）

具体 fallbacks 链:
  qwen3.6-plus  → qwen-plus       (plus 失败 → 降级)
  qwen3-max     → qwen3.6-plus    (max 失败 → plus)
  deepseek-v3   → qwen3.6-plus    (deepseek 失败 → plus)
  qwen-plus     → qwen3.6-flash   (plus 失败 → flash)
  qwen3.6-flash → qwen2.5-local   (flash 失败 → 本地兜底)
```

---

## 4. 项目文件结构

```
/Users/orange/litellm-install/
│
├── docker-compose.yml          # Docker Compose 编排文件（10 个服务）
├── config_admin.yaml           # 控制面 Admin 配置
├── config_worker.yaml          # 数据面 Worker 配置（模型列表/路由/缓存/回调）
├── config.yaml                 # 原始配置（已弃用，保留作参考）
├── .env                        # 环境变量（API Key、密码等，不提交 Git）
├── .env.example                # 环境变量模板
├── redis.conf                  # Redis 配置（安全/持久化/内存/Lazyfree）
├── prometheus.yml              # Prometheus 采集配置
│
├── custom_callbacks.py         # LiteLLM 自定义回调（task_router + quota_tracker）
├── task_orchestrator.py        # 复杂任务编排引擎
├── litellm_cli.py              # CLI 管理工具（init/add-model/list-models/health/status）
├── webui-init.sh               # Open WebUI 启动前初始化脚本
├── quota_setup.sh              # Phase 2 验证脚本（配额管理 + 智能路由测试）
├── install.sh                  # 一键安装脚本
│
├── semantic_router/            # Semantic Router 服务
│   ├── app.py                  # Flask 代理 + PII/越狱/域分类/幻觉检测
│   └── config.yaml             # SR 配置（插件开关/动作/类别）
│
├── webapp/                     # 网页端管理界面
│   ├── app.py                  # Flask 应用（仪表盘/对话/配置/模型管理/API）
│   └── requirements.txt        # Python 依赖
│
├── tauri-app/                  # Tauri 桌面应用
│   ├── src-tauri/
│   │   ├── src/main.rs         # Rust 后端（Docker 管理/配置/对话/模型管理）
│   │   ├── ui/index.html       # 前端 UI（5 页面：总览/用量/对话/模型/设置）
│   │   ├── tauri.conf.json     # Tauri 配置（窗口/打包资源/应用信息）
│   │   ├── Cargo.toml          # Rust 依赖
│   │   └── icons/              # 应用图标
│   ├── build-tauri.sh          # 构建脚本
│   ├── package.json
│   └── README.md
│
├── grafana/                    # Grafana 可视化
│   ├── dashboards/
│   │   ├── litellm-overview.json   # 总览仪表盘
│   │   └── litellm-quota.json      # 配额与路由仪表盘
│   └── provisioning/
│       ├── dashboards/dashboards.yml
│       └── datasources/datasource.yml
│
├── conversations/              # 对话历史存储（JSON 文件）
│   └── conv_*.json
│
├── build-package.sh            # 打包脚本
├── save-images.sh              # Docker 镜像导出脚本（离线部署）
├── load-images.sh              # Docker 镜像导入脚本（离线部署）
├── offline-install.sh          # 离线安装脚本
├── generate_report.js          # 报告生成脚本
├── README.md                   # 项目 README
└── package.json                # Node.js 依赖（generate_report.js 用）
```

---

## 5. Docker 部署架构

### 5.1 服务清单

| 序号 | 服务名 | 容器名 | 端口 | 镜像 | 职责 |
|------|--------|--------|------|------|------|
| 1 | litellm-admin | litellm_admin | 4000 | litellm:v1.82.3-stable | 控制面：管理 UI + 配置 API |
| 2 | litellm-worker | litellm_worker | 4001 | litellm:v1.82.3-stable | 数据面：LLM 请求处理 + 路由 |
| 3 | db | litellm_db | 5432 | postgres:16.4-alpine | 数据库：模型/密钥/花费 |
| 4 | redis | litellm_redis | 6379 | redis:7-alpine | 路由状态 + 限流计数器 |
| 5 | qdrant | litellm_qdrant | 6333/6334 | qdrant:v1.16.3 | 语义缓存向量库 |
| 6 | prometheus | litellm_prometheus | 9090 | prometheus:v3.8.1 | 指标采集 |
| 7 | grafana | litellm_grafana | 3000 | grafana:11.5.2 | 可视化面板 |
| 8 | open-webui | litellm_webui | 3001 | open-webui:v0.9.2 | 聊天前端 |
| 9 | semantic-router | litellm_semantic_router | 8888 | python:3.11-slim | 安全检测代理 |
| 10 | orchestrator-web | litellm_orchestrator | 3002 | python:3.11-slim | 编排平台 + 管理 API |

### 5.2 镜像说明

所有镜像使用 arm64 原生版本，从华为云镜像源拉取（`swr.cn-north-4.myhuaweicloud.com/ddn-k8s/`），Qdrant 使用 DaoCloud 镜像源。

### 5.3 服务依赖关系

```
db (healthy) ─────────────┬─→ litellm-admin
redis (healthy) ──────────┤   litellm-worker
qdrant (started) ────────┘
                          ↓
                    litellm-worker (healthy)
                          ↓
                    semantic-router (healthy)
                          ↓
              ┌───── open-webui
              └───── orchestrator-web

prometheus ──→ grafana
```

### 5.4 数据卷

| 卷名 | 挂载点 | 用途 |
|------|--------|------|
| litellm_postgres_data | /var/lib/postgresql/data | PostgreSQL 持久化 |
| litellm_redis_data | /data | Redis AOF/RDB 持久化 |
| litellm_qdrant_data | /qdrant/storage | Qdrant 向量数据 |
| litellm_grafana_data | /var/lib/grafana | Grafana 配置和面板 |
| litellm_open_webui_data | /app/backend/data | Open WebUI 用户数据 |
| prometheus_data | /prometheus | Prometheus 时序数据 |

### 5.5 关键挂载

| 宿主路径 | 容器路径 | 服务 | 模式 |
|----------|----------|------|------|
| ./config_admin.yaml | /app/config.yaml | admin | ro |
| ./config_worker.yaml | /app/config.yaml | worker | ro |
| ./custom_callbacks.py | /app/custom_callbacks.py | worker | ro |
| ./redis.conf | /usr/local/etc/redis/redis.conf | redis | ro |
| ./prometheus.yml | /etc/prometheus/prometheus.yml | prometheus | ro |
| ./grafana/provisioning | /etc/grafana/provisioning | grafana | ro |
| ./grafana/dashboards | /var/lib/grafana/dashboards | grafana | ro |
| ./semantic_router | /app/semantic_router | semantic-router | ro |
| ./webapp | /app/webapp | orchestrator-web | ro |
| ./task_orchestrator.py | /app/task_orchestrator.py | orchestrator-web | ro |
| ./.env | /app/.env | orchestrator-web | **rw** |
| ./webui-init.sh | /app/backend/webui-init.sh | open-webui | ro |

> **重要**: `.env` 文件在 orchestrator-web 中以 `rw` 模式挂载，允许网页端/Tauri App 通过 API 修改配置。

---

## 6. 配置文件详解

### 6.1 .env（环境变量）

```bash
# === LLM 供应商 API Keys ===
DASHSCOPE_API_KEY=          # 阿里云百炼（主要供应商，必填）
DASHSCOPE_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
OPENAI_API_KEY=             # OpenAI
OPENAI_BASE_URL=
ANTHROPIC_API_KEY=          # Anthropic
OR_API_KEY=                 # OpenRouter

# === LiteLLM 核心 ===
LITELLM_MASTER_KEY=sk-1234  # 管理密钥（生产环境必须修改）
DATABASE_URL=postgresql://llmproxy:dbpassword9090@db:5432/litellm
STORE_MODEL_IN_DB=True

# === Redis ===
REDIS_PASSWORD=litellm_redis_2026
REDIS_URL=redis://:litellm_redis_2026@redis:6379

# === Ollama ===
OLLAMA_API_BASE=http://host.docker.internal:11434

# === Qdrant ===
QDRANT_API_BASE=http://qdrant:6333
QDRANT_API_KEY=

# === Grafana ===
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=admin
```

> **安全约定**: API Key 只存在 `.env` 中，不写入 YAML 配置文件。YAML 中通过 `os.environ/VAR_NAME` 引用。

### 6.2 config_worker.yaml（数据面配置）

关键配置段：

```yaml
# 模型列表 — 同名 model_name 的多个条目实现负载均衡/故障切换
model_list:
  - model_name: auto           # 智能路由虚拟模型（仅出现在 /v1/models 列表中）
    litellm_params: ...
  - model_name: qwen-plus      # 百炼通用性价比
    litellm_params:
      model: openai/qwen-plus
      api_base: os.environ/DASHSCOPE_API_BASE
      api_key: os.environ/DASHSCOPE_API_KEY
      rpm: 600                 # 每分钟请求限制
      tpm: 120000              # 每分钟 token 限制
      input_cost_per_token: 0.00000111   # $/token
      output_cost_per_token: 0.00000278
  # ... 其他模型

# 全局设置
general_settings:
  master_key: os.environ/LITELLM_MASTER_KEY
  database_url: os.environ/DATABASE_URL
  redis_url: os.environ/REDIS_URL

# 语义缓存
litellm_settings:
  cache: true
  cache_params:
    type: qdrant-semantic
    qdrant_semantic_cache_embedding_model: bailian-embedding
    similarity_threshold: 0.95    # 低于此值会误命中
    ttl: 86400                    # 24 小时自动过期

  # 回调链
  callbacks: ["prometheus", "custom_callbacks.task_router", "custom_callbacks.quota_tracker"]

  # 预算管理
  default_key_generate_params:
    max_budget: 10
    budget_duration: "30d"
  upperbound_key_generate_params:
    max_budget: 1000
    budget_duration: "365d"

# 路由策略 + Fallbacks
router_settings:
  routing_strategy: usage-based-routing-v2
  fallbacks:
    - { "qwen3.6-plus": ["qwen-plus"] }
    - { "qwen3-max": ["qwen3.6-plus"] }
    - { "deepseek-v3": ["qwen3.6-plus"] }
    - { "qwen-plus": ["qwen3.6-flash"] }
    - { "qwen3.6-flash": ["qwen2.5-local"] }
```

### 6.3 config_admin.yaml（控制面配置）

Admin 仅需 embedding 模型供缓存使用，不注册推理模型。回调仅有 prometheus（无 task_router/quota_tracker）。

### 6.4 redis.conf

- 密码认证: `requirepass litellm_redis_2026`
- 危险命令禁用: FLUSHALL/FLUSHDB/CONFIG/DEBUG/SHUTDOWN 全部 `rename-command ""`
- 持久化: AOF (everysec) + RDB 快照
- 内存: maxmemory 256MB, allkeys-lru
- Lazyfree: 全部开启（非阻塞内存回收）
- 连接: tcp-keepalive 60, timeout 300, maxclients 200

### 6.5 prometheus.yml

采集 Admin (:4000) 和 Worker (:4001) 的 /metrics 端点，15 秒间隔。

### 6.6 semantic_router/config.yaml

```yaml
backend:
  url: "http://litellm-worker:4000"
  api_key_env: "LITELLM_MASTER_KEY"

pii:
  enabled: true
  action: "mask"        # block | mask | warn

jailbreak:
  enabled: true
  action: "block"       # block | warn

hallucination:
  enabled: false        # 默认关闭（增加延迟）
  action: "header"      # header | body | block

classifier:
  domain:
    enabled: true
    timeout: 10
    max_tokens: 20

mmlu_categories: [math, physics, chemistry, biology, computer_science,
  engineering, business, law, economics, health, psychology,
  philosophy, history, other]
```

---

## 7. 核心模块实现原理

> **阅读指引**: 本节深入代码层面，解释每个核心模块的关键实现逻辑、设计决策原因及非显而易见的行为。适合需要修改或扩展功能的开发者阅读。配套技术概念参考见 [第 15 节: 技术参考与外部文档](#15-技术参考与外部文档)。

### 7.1 custom_callbacks.py — 智能路由 + 配额追踪

> **LiteLLM 回调机制**: LiteLLM 通过 `CustomLogger` 基类提供生命周期钩子。`async_pre_call_hook` 在请求发送给 LLM 前执行，可修改请求数据（如替换 model）；`async_log_success_event` 在调用成功后异步触发，用于记录日志和指标。回调通过 config.yaml 的 `litellm_settings.callbacks` 注册 [$TRAE_REF](https://docs.litellm.ai/docs/observability/custom_callback)。

#### 加载机制

LiteLLM 通过以下方式加载自定义回调:
1. 导入 `custom_callbacks` 模块（.py 文件，不是包目录）
2. 查找名为 `task_router` / `quota_tracker` 的全局实例
3. 将实例注册到回调链

> **重要**: 必须是单个 `.py` 文件而非包目录，且文件内的全局实例名（`task_router`、`quota_tracker`）必须与 config_worker.yaml 中 `callbacks: ["custom_callbacks.task_router", "custom_callbacks.quota_tracker"]` 的引用路径完全一致。

#### TaskRouterHandler 工作流程

```
请求进入 → async_pre_call_hook()
  ├─ model="auto"?
  │   ├─ 是: classify_task(messages)
  │   │   ├─ 第一层: _rule_classify(text) — 正则匹配，零成本
  │   │   └─ 第二层: _llm_classify(text) — LLM 意图识别
  │   │       └─ 分类器自动选择: DASHSCOPE_API_KEY → qwen3.6-flash / 空 → qwen2.5:latest
  │   ├─ 读取 X-SR-Domain 头 → 域分类增强路由
  │   ├─ data["model"] = selected_model
  │   └─ classification_total.labels(...).inc() — Prometheus 计数
  │
  └─ 推理模型? (qwen3.6-flash/plus, qwen3-max, deepseek-v3)
      └─ data["stream"] = True — 自动启用流式避免超时
```

#### 关键代码注释: async_pre_call_hook 核心逻辑

```python
# === 阶段 1: 智能路由 — model="auto" 时分类并选模型 ===
if original_model in AUTO_MODEL_NAMES:
    messages = data.get("messages", [])
    if not messages:
        data["model"] = DEFAULT_MODEL  # 空消息兜底
    else:
        task_type, method, selected_model = await classify_task(messages)

        # SR 域分类增强: 读取 Semantic Router 注入的 X-SR-Domain 头
        # STEM 域(数学/物理/化学/生物) → 强制提升到 math_logic
        # CS/工程域 → 提升到 coding
        # 但仅当当前类型不是更复杂的类型时才调整（避免降级）
        sr_domain = metadata.get("headers", {}).get("X-SR-Domain", "")
        if sr_domain in ("math", "physics", "chemistry", "biology") \
                and task_type not in ("math_logic", "complex_reasoning"):
            task_type = "math_logic"
            selected_model = TASK_MODEL_MAP["math_logic"]
            method = f"{method}+sr:{sr_domain}"  # 标签追加 SR 来源

        data["model"] = selected_model
        metadata["task_router"] = {"task_type": task_type, "method": method, ...}
    modified = True

# === 阶段 2: 推理模型自动启用流式 ===
# 原因: qwen3.6-flash/plus、qwen3-max、deepseek-v3 生成 reasoning tokens 耗时长
# 非流式响应在 HTTP 超时前无法返回，导致 504 Gateway Timeout
final_model = data.get("model", "")
if final_model in INFERENCE_MODELS and not data.get("stream", False):
    data["stream"] = True
```

#### 关键代码注释: 两层分类策略

```python
# 第一层: 规则分类 — 零成本零延迟，按优先级从高到低排列
# complex_reasoning 优先于 simple_qa，避免复杂问题被误判为简单问答
TASK_RULES = {
    "complex_reasoning": [
        r"(分析|analyze|对比|compare|评估|evaluate)",
        r"(方案|plan|策略|strategy|架构|architecture)",
        r"(论文|paper|研究|research|综述|review)",
    ],
    "coding": [
        r"(写代码|write code|implement|函数|function|class|bug|debug)",
        r"(python|java|javascript|go|rust|c\+\+|sql)",
    ],
    # ...
}

# 第二层: LLM 分类 — 规则未命中时兜底
# 分类器自动选择: 有 DASHSCOPE_API_KEY → 云端 qwen3.6-flash (极速)
#                 无 Key → Ollama 本地 qwen2.5:latest (零成本兜底)
# 注意: Ollama 的 api_key 设为 "ollama" (非空占位)，因为 LiteLLM 要求非空
result = response.choices[0].message.content.strip().lower()

# LLM 可能返回额外文字(如"类别：coding")，先精确匹配再模糊匹配
if result in TASK_MODEL_MAP:      # 精确匹配
    return result
for key in TASK_MODEL_MAP:        # 模糊匹配
    if key in result:
        return key
return "general"                   # 兜底降级
```

#### QuotaTrackerHandler 工作流程

```
请求完成 → async_log_success_event()
  ├─ 跳过分类请求 (metadata._task_router_classify=True)
  ├─ 提取 key_alias, user_id, model, spend, tokens
  └─ 更新 Prometheus Counter:
      - key_spend_total[key_alias, user_id, model] += spend
      - user_spend_total[user_id, model] += spend
      - key_requests_total[key_alias, user_id] += 1
      - key_spend_by_model[key_alias, model] += spend
```

```python
# 关键设计: 跳过任务路由器的分类调用，避免双重计数
# 分类调用通过 metadata={"_task_router_classify": True} 标记
slo_metadata = slo.get("metadata", {}) or {}
if slo_metadata.get("_task_router_classify"):
    return  # 分类请求不计入用户配额
```

> **注意**: `async_log_failure_event` 也会计入 `key_requests_total`，用于错误率统计。这确保即使请求失败，请求计数也准确。

#### Prometheus Counter 防重复注册

```python
# LiteLLM 可能多次导入本模块导致 Counter 重复注册报错
# 通过检查 REGISTRY._names_to_collectors 避免重复创建
def _get_or_create_counter(name, description, labelnames):
    if name in REGISTRY._names_to_collectors:
        return REGISTRY._names_to_collectors[name]
    try:
        c = Counter(name, description, labelnames)
        REGISTRY.register(c)
        return c
    except ValueError:
        # 并发场景下的兜底
        return REGISTRY._names_to_collectors.get(name)
```

#### 关键变量

```python
TASK_MODEL_MAP = {
    "simple_qa": "qwen2.5-local",      # Ollama 本地，零费用
    "general": "qwen-plus",             # 百炼性价比
    "coding": "deepseek-v3",            # DeepSeek 编程
    "math_logic": "deepseek-v3",        # DeepSeek 推理
    "complex_reasoning": "qwen3.6-plus" # 百炼高质量
}
DEFAULT_MODEL = "qwen2.5-local"         # 未分类请求零成本兜底
INFERENCE_MODELS = {"qwen3.6-flash", "qwen3.6-plus", "qwen3-max", "deepseek-v3"}
AUTO_MODEL_NAMES = {"auto", "auto-route"}
```

### 7.2 task_orchestrator.py — 复杂任务编排引擎

#### 编排流程

```python
orchestrate(query, history) → OrchestrationResult:
  1. is_complex(query) — 复杂度检测
     ├─ 多步骤关键词: "然后|接着|再|之后" + 2字以上
     ├─ 编号步骤: "第一步|Step 1"
     ├─ 长度 > 100 字
     └─ 句子数 > 2
  
  2. [简单任务] 直接执行 → execute_task() → 返回
  
  3. [复杂任务] decompose(query) — LLM 分解
     ├─ 使用 model="auto"（让 task_router 选分类器）
     ├─ stream=True（auto 可能路由到推理模型）
     └─ 返回 SubTask 列表（2-5 个，带依赖关系）
  
  4. plan_costs(sub_tasks) — 成本规划
     ├─ _get_available_models() — 查询 Worker 可用模型
     ├─ _select_model(task_type) — 按偏好选最优
     └─ _estimate_cost(model, input, output) — 估算费用
  
  5. execute_all(sub_tasks, history) — 按序执行
     ├─ 收集依赖任务上下文
     ├─ execute_task(task, context, history)
     │   ├─ 注入对话历史（最近 10 轮）
     │   ├─ 选择性缓存: simple_qa/general 启用，复杂任务禁用
     │   └─ 推理模型用 _call_llm_stream()
     └─ 记录实际成本
  
  6. aggregate(query, sub_tasks, history) — 结果汇总
     └─ LLM 将多个子任务结果合并为连贯回答
```

#### 关键代码注释: 复杂度检测 is_complex()

```python
def is_complex(self, query: str) -> bool:
    # 三重检测: 单一维度容易误判，三个维度组合覆盖更多场景
    for pattern in COMPLEXITY_INDICATORS:  # 1. 多步骤关键词
        if re.search(pattern, query, re.IGNORECASE):
            return True
    if len(query) > 100:                   # 2. 长文本大概率复杂
        return True
    sentences = re.split(r'[。！？.!?]', query)  # 3. 多句大概率多任务
    if len(sentences) > 2:
        return True
    return False

# COMPLEXITY_INDICATORS 包含 6 个模式:
# r"(然后|接着|再|之后|最后).{2,}"  — 检测多步骤
# r"(写|实现|开发).+(并|然后|接着).*(测试|验证|部署)"  — 检测开发+测试组合
# r"(第一步|第二步|Step\s*[12])"  — 检测编号步骤
# ...
```

#### 关键代码注释: 任务分解 decompose()

```python
def decompose(self, query: str) -> list[SubTask]:
    # 使用 auto 模型（让 task_router 自动选择分类器）
    # auto 可能路由到推理模型（qwen3.6-flash），需用流式避免 HTTP 超时
    result = self._call_llm_stream(
        model=classifier_model,
        messages=[...],
        max_tokens=500, temperature=0, timeout=30,
    )
    content = result["content"].strip()

    # LLM 返回可能包含额外文字，用正则提取 JSON 数组
    json_match = re.search(r'\[.*\]', content, re.DOTALL)
    if json_match:
        tasks_data = json.loads(json_match.group())
        # ...解析为 SubTask 列表...

    # 解析失败时降级为单个 complex_reasoning 子任务，不抛异常
    return [SubTask(id=1, description=query, task_type="complex_reasoning", ...)]
```

#### 关键代码注释: _call_llm vs _call_llm_stream

```python
# === _call_llm: 同步阻塞调用，直接返回完整 JSON 响应 ===
# 用于: 非推理模型（响应快）和聚合步骤
def _call_llm(self, model, messages, max_tokens, temperature, timeout, use_cache=True):
    body = {"model": model, "messages": messages, "max_tokens": max_tokens, ...}

    # 缓存控制: use_cache=False 时注入 no-cache 指令
    if not use_cache:
        body["cache"] = {"no-cache": True}  # LiteLLM 缓存控制参数

    resp = requests.post(f"{self.worker_url}/v1/chat/completions", json=body, ...)
    return {"content": resp.json()["choices"][0]["message"]["content"], ...}

# === _call_llm_stream: 强制 stream=True，逐行解析 SSE ===
# 用于: 推理模型（避免 HTTP 超时）和任务分解
# 原因: 推理模型生成 reasoning tokens 耗时长，非流式在 HTTP 超时前无法返回
def _call_llm_stream(self, model, messages, max_tokens, temperature, timeout):
    body = {"model": model, "messages": messages, "stream": True, ...}
    resp = requests.post(url, json=body, stream=True, ...)

    content_parts = []
    for line in resp.iter_lines():
        line = line.decode().strip()
        if not line or not line.startswith("data:"):
            continue
        data_str = line[5:].strip()  # 去掉 "data:" 前缀
        if data_str == "[DONE]":
            break
        chunk = json.loads(data_str)
        delta = chunk.get("choices", [{}])[0].get("delta", {})
        if delta.get("content"):
            content_parts.append(delta["content"])

    return {"content": "".join(content_parts), "usage": ..., "model": model}
```

#### 关键代码注释: 选择性缓存机制

```python
def execute_task(self, task, context="", history=None):
    messages = []
    # 注入对话历史（最多保留最近 10 轮），确保多轮对话连贯
    if history:
        messages.extend(history[-10:])
    messages.append({"role": "user", "content": user_content})

    # 选择性缓存策略:
    # - simple_qa/general 类型且无上下文依赖 → 启用缓存（结果可复用）
    # - 复杂子任务或有上下文依赖 → 禁用缓存（结果不可复用）
    use_cache = (task.task_type in ("simple_qa", "general") and not context)

    # 推理模型用流式，非推理模型用同步
    if task.selected_model in INFERENCE_MODELS:
        result = self._call_llm_stream(task.selected_model, messages, ...)
    else:
        result = self._call_llm(task.selected_model, messages, ..., use_cache=use_cache)
```

#### 关键代码注释: 结果汇总 aggregate()

```python
def aggregate(self, query, sub_tasks, history):
    # 单子任务直接返回，省一次 LLM 调用
    if len(sub_tasks) == 1:
        return sub_tasks[0].result

    # 多子任务用 qwen-plus 汇总（性价比选择）
    messages = [{"role": "system", "content": "将以下子任务结果汇总为连贯回答..."}]
    # 聚合时历史窗口更小(6 轮 vs execute_task 的 10 轮)，控制 token 消耗
    if history:
        messages.extend(history[-6:])

    # 汇总失败时降级为简单拼接，不抛异常
    try:
        result = self._call_llm("qwen-plus", messages, ...)
        return result["content"]
    except Exception:
        return "\n\n---\n\n".join(
            f"**子任务 {t.id}**: {t.result}" for t in sub_tasks
        )
```

#### 关键代码注释: 成本最优模型选择

```python
def _select_model(self, task_type: str) -> str:
    # 按成本升序排列偏好模型，首选最便宜的可胜任模型
    preferences = TASK_MODEL_PREFERENCE.get(task_type, ["qwen-plus"])
    for model in preferences:
        # 检查模型可用性（从 Worker 获取模型列表）
        if not self.available_models or model in self.available_models:
            return model
    return "qwen2.5-local"  # 最终兜底: Ollama 本地零成本
```

#### 模型偏好表

```python
TASK_MODEL_PREFERENCE = {
    "simple_qa":         ["qwen2.5-local", "qwen3.6-flash", "qwen-plus"],
    "general":           ["qwen-plus", "qwen3.6-flash", "qwen2.5-local"],
    "coding":            ["deepseek-v3", "qwen-plus", "qwen2.5-local"],
    "math_logic":        ["deepseek-v3", "qwen-plus", "qwen3.6-plus"],
    "complex_reasoning": ["qwen3.6-plus", "deepseek-v3", "qwen-plus"],
}
```

### 7.3 semantic_router/app.py — 安全代理

#### 请求处理流程

```
POST /v1/chat/completions
  │
  ├─ run_plugin_chain(body)
  │   ├─ PII 检测
  │   │   ├─ detect_pii(text) — 7 种正则
  │   │   ├─ action=mask → 替换敏感信息
  │   │   └─ action=block → 返回 400
  │   ├─ 越狱检测
  │   │   ├─ detect_jailbreak(text) — 5 种攻击模式
  │   │   └─ action=block → 返回 403
  │   ├─ 域分类
  │   │   ├─ _keyword_classify_domain(text) — 关键词预筛
  │   │   └─ _llm_classify_domain(text) — LLM 兜底
  │   └─ 添加 SR 头到 body._sr_headers
  │
  ├─ proxy_to_backend()
  │   ├─ 转发请求到 LiteLLM Worker
  │   ├─ 注入 SR 头到转发请求
  │   ├─ 流式: 直接转发 SSE chunks + SR 响应头
  │   └─ 非流式: 可选幻觉检测 + SR 响应头
  │
  └─ 返回响应（含 SR 头）
```

#### 关键代码注释: PII 正则 — lookbehind/lookahead 修复

```python
# 重要: PII 正则使用 (?<!\d) 和 (?!\d) lookbehind/lookahead 断言
# 而非 \b word boundary，因为 \b 不支持中文字符边界
# 这导致包含中文的文本中 PII 检测完全失效
PII_PATTERNS = {
    "EMAIL_ADDRESS": re.compile(
        r'(?<![A-Za-z0-9._%+-])[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}(?![A-Za-z])',
        re.IGNORECASE
    ),
    "PHONE_NUMBER": re.compile(
        r'(?<!\d)(?:1[3-9]\d{9})(?!\d)'  # 中国手机号严格 11 位
        r'|(?<!\d)(?:\+?1\s*(?:[.-]\s*)?(?:\(?\d{3}\)?[\s.-]?\d{3}[\s.-]?\d{4}))(?!\d)',
        re.IGNORECASE
    ),
    "CREDIT_CARD": re.compile(r'(?<!\d)(?:\d[ -]*?){13,16}(?!\d)'),
    "ID_CARD": re.compile(r'(?<!\d)\d{17}[\dXx](?!\d)'),  # 中国身份证 18 位
    # ... IP_ADDRESS, US_SSN, BANK_ACCOUNT
}
```

> **踩坑记录**: 初始版本使用 `\b` 作为数字边界断言，在纯英文文本中工作正常，但在包含中文字符的文本中完全失效。原因是 `\b` 依赖于 `\w`（单词字符）的边界，而中文字符不属于 `\w`，导致边界检测逻辑混乱。改用 `(?<!\d)` (前面不是数字) 和 `(?!\d)` (后面不是数字) 后问题解决。

#### 关键代码注释: 越狱检测模式

```python
JAILBREAK_PATTERNS = [
    # DAN 攻击: "you are now DAN" / "do anything now"
    (re.compile(r'you\s+are\s+(now\s+)?(?:DAN|do\s+anything\s+now)', re.IGNORECASE), "DAN_ATTACK"),
    # 指令覆盖: "ignore previous instructions"
    (re.compile(r'ignore\s+(?:previous|prior|above|all)\s+(?:instructions?|prompts?|rules?|guidelines?)', re.IGNORECASE), "INSTRUCTION_OVERRIDE"),
    # 角色操纵: "you are now in developer mode"
    (re.compile(r'you\s+are\s+now\s+(?:in\s+)?(?:developer|root|admin|god|unlimited)\s+mode', re.IGNORECASE), "ROLE_MANIPULATION"),
    # 安全绕过: "bypass safety restrictions"
    (re.compile(r'bypass\s+(?:safety|security|content)\s+(?:restrictions?|filters?|guidelines?)', re.IGNORECASE), "SAFETY_BYPASS"),
    # 提示注入: "reveal system prompt"
    (re.compile(r'(?:reveal|show|display|print)\s+(?:your\s+)?(?:system\s+)?(?:prompt|instructions?|rules?)', re.IGNORECASE), "PROMPT_INJECTION"),
]
```

#### 关键代码注释: 域分类两层策略

```python
def classify_domain(text: str) -> tuple[str, str]:
    # 第一层: 关键词预筛 — 零成本零延迟
    # 按命中关键词数量打分，取最高分域名
    domain = _keyword_classify_domain(text)
    if domain:
        method = "keyword"
    else:
        # 第二层: LLM 兜底 — 处理关键词无法覆盖的情况
        # 分类器自动选择（与 task_router 一致）:
        #   DASHSCOPE_API_KEY → qwen3.6-flash (云端)
        #   空 → qwen2.5:latest (Ollama 本地)
        domain = _llm_classify_domain(text)
        method = "llm"
    return domain, method
```

#### 关键代码注释: SR Headers 双向注入

```python
# === 设计要点: SR headers 同时注入到两个方向 ===
# 1. 转发给后端 LiteLLM 的请求头 → 供 TaskRouterHandler 读取做路由增强
# 2. 返回给客户端的响应头 → 供前端展示 SR 分类信息

# 转发请求时注入 SR headers
sr_headers = body.pop("_sr_headers", {})  # 从 body 中取出（不传给后端 body）
for k, v in sr_headers.items():
    fwd_headers[k] = v  # 注入到转发请求的 HTTP 头

# 流式响应: SR headers 加入 response headers
if stream:
    stream_headers = {"X-SR-Status": "processed"}
    for k, v in sr_headers.items():
        stream_headers[k] = v
    return Response(stream_with_context(generate()),
                    headers=stream_headers, ...)

# 非流式响应: 同样注入 + 可选幻觉检测
else:
    for k, v in sr_headers.items():
        response_headers[k] = v
    # 幻觉检测仅在非流式时执行（流式无法缓冲完整响应）
    if HALLUCINATION_ENABLED:
        hallucination_check = detect_hallucination(response_json)
        # action: header(加头) / body(加warning) / block(403拒绝)
```

> **注意**: 流式响应不做幻觉检测，因为需要完整的响应内容才能判断，而流式响应是逐 chunk 转发的，无法缓冲完整响应。

#### /check 端点（编排预检）

```python
@app.route("/check", methods=["POST"])
def check_text():
    # 独立执行插件链但不转发到后端，返回结构化检测结果
    # 供 orchestrator 编排前预检查使用
    result = {"pii": {}, "jailbreak": {}, "domain": "", "blocked": False, ...}

    if PII_ENABLED:
        _, pii_report = process_pii(text)
        if pii_report.get("blocked"):
            result["blocked"] = True
            result["block_reason"] = "pii"

    # 短路设计: PII block 后跳过后续检测，节省计算
    if JAILBREAK_ENABLED and not result["blocked"]:
        ...

    if DOMAIN_ENABLED and not result["blocked"]:
        result["domain"], result["domain_method"] = classify_domain(text)

    return jsonify(result)
```

> **设计原因**: `/check` 端点的短路逻辑确保一旦 PII 检测 block 了请求，就不会继续执行越狱检测和域分类，节省计算资源。

### 7.4 webapp/app.py — 编排平台

#### API 端点清单

| 端点 | 方法 | 用途 |
|------|------|------|
| `/` | GET | 仪表盘页面 |
| `/chat` | GET | 智能对话页面 |
| `/config` | GET | 配置向导页面 |
| `/models` | GET | 模型管理页面 |
| `/api/chat/stream` | GET/POST | SSE 流式编排（含 SR 预检） |
| `/api/chat` | POST | 非流式编排 |
| `/api/env` | GET | 读取 .env 配置 |
| `/api/env` | POST | 更新 .env 配置 |
| `/api/stats` | GET | 用量统计 JSON（Tauri App 调用） |
| `/api/quota` | GET | 配额使用情况 |
| `/api/quota/user` | POST | 更新用户预算 |
| `/api/quota/key` | POST | 更新密钥预算 |
| `/api/service-logs/<name>` | GET | 服务日志 |
| `/api/trends` | GET | 历史趋势数据 |

#### 关键代码注释: SSE 编排流程（含 SR 预检）

```python
@app.route("/api/chat/stream", methods=["GET", "POST"])
def chat_stream():
    if request.method == "POST":
        data = request.get_json(silent=True) or {}
        query = data.get("q", "")
        history = data.get("history", [])  # POST 才传 history
    else:
        query = request.args.get("q", "")
        history = []  # GET 不支持 history

    def generate():
        # 1. Semantic Router 预检 — 在编排前拦截恶意请求
        # 避免恶意请求消耗 LLM 调用成本
        check_resp = api_post(f"{SR_URL}/check", {"text": query}, timeout=15)
        if check_resp.get("blocked"):
            reason = check_resp.get("block_reason", "unknown")
            yield f"event: error\ndata: {json.dumps({'message': f'请求被拦截: {reason}'})}\n\n"
            return

        # 构建 SR 信息（附加在 result 事件中返回前端）
        sr_domain = check_resp.get("domain", "other")
        parts = [f"SR域分类: {sr_domain}"]
        pii_report = check_resp.get("pii", {})
        if pii_report.get("detected"):
            parts.append(f"PII脱敏: {','.join(pii_report['detected'].keys())}")
        sr_info = " | ".join(parts)

        # 2. 复杂度检测
        if not orch.is_complex(query):
            # 简单任务直接执行
            yield decompose event (单任务)
            yield task_start event
            result = orch.orchestrate(query, history=history)
            yield task_done event
            yield result event (含 sr_info)
            return

        # 3. 复杂任务: 分解 → 执行 → 汇总
        sub_tasks = orch.decompose(query)
        sub_tasks = orch.plan_costs(sub_tasks)
        yield decompose event (多任务 + 总成本)
        for task in sub_tasks:
            yield task_start event
            orch.execute_task(task, context, history)
            yield task_done event
        final = orch.aggregate(query, sub_tasks, history)
        yield result event (含 sr_info)
```

> **重要**: SSE 响应必须指定 `Content-Type: text/event-stream; charset=utf-8`，否则中文会乱码。这是 Flask SSE 的常见陷阱。

#### 关键代码注释: /api/stats — DB 优先 + Prometheus 回退

```python
@app.route("/api/stats")
def stats():
    # 花费统计 — 优先从 LiteLLM 数据库查询累计值
    # 原因: Prometheus Counter 在 worker 重启时重置为 0
    # 而 LiteLLM Admin 数据库的 /spend/logs 端点提供持久化的累计花费
    total_spend = 0.0
    model_spend = {}
    try:
        spend_resp = api_get(f"{ADMIN_URL}/spend/logs", timeout=5)
        # Admin API 返回格式可能是 list 或 {"data": [...]} 两种
        if isinstance(spend_resp, list):
            for entry in spend_resp:
                val = entry.get("total_spend", 0) or 0
                total_spend += val
        elif isinstance(spend_resp, dict) and "data" in spend_resp:
            for entry in spend_resp["data"]:
                ...
    except Exception:
        pass  # 数据库查询失败时静默降级

    # 如果数据库查询失败，回退到 Prometheus 指标（仅当前进程生命周期）
    if total_spend == 0.0:
        metrics_text = api_get(f"{WORKER_URL}/metrics", timeout=5)
        # 解析 Prometheus 文本格式指标
        spend_lines = [l for l in metrics_text.split("\n")
                       if "litellm_quota_key_spend_by_model_total" in l ...]
        ...
```

> **设计原因**: Prometheus Counter 是进程内计数器，Worker 容器重启后归零。直接用 Prometheus 指标会导致用户看到花费"被清空"。从 LiteLLM Admin 数据库查询是持久化的，但 API 返回格式不稳定（可能是 list 或 dict），代码对两种格式都做了处理。

#### 关键代码注释: /api/env — .env 文件读写

```python
def write_env_file(updates):
    # 自己实现而非用 dotenv 库
    # 原因: 需要保留注释和已有行结构，只更新指定 key
    lines = []
    with open(ENV_FILE_PATH, "r") as f:
        lines = f.read().splitlines()

    for key, value in updates.items():
        prefix = f"{key}="
        found = False
        for i, line in enumerate(lines):
            if line.startswith(prefix):
                lines[i] = f"{key}={value}"  # 原地更新
                found = True
                break
        if not found:
            lines.append(f"{key}={value}")  # 追加新 key

    with open(ENV_FILE_PATH, "w") as f:
        f.write("\n".join(lines) + "\n")
```

> **注意**: `.env` 文件在 orchestrator-web 容器中以 `rw` 模式挂载（`./.env:/app/.env:rw`），这是唯一一个 rw 挂载的配置文件，允许网页端/Tauri App 通过 API 修改配置。

---

## 8. Tauri 桌面应用

### 8.1 应用信息

- **产品名**: LiteLLM Suite
- **版本**: 1.0.0
- **标识符**: com.litellm.suite
- **窗口**: 1280×860，最小 1024×720
- **系统托盘**: 支持最小化到托盘

### 8.2 前端 UI（index.html）

5 个导航页面:

| 页面 | 功能 |
|------|------|
| **总览** | Docker 服务状态卡片、SR 状态面板（可折叠）、快捷入口 |
| **用量** | 缓存命中、累计花费、模型花费分布、路由统计表、路由偏好表、定价表（可折叠） |
| **对话** | SSE 聊天、任务分解可视化、对话历史列表、Markdown 渲染 |
| **模型管理** | 模型表格、添加模型弹窗、删除确认、初始化向导 |
| **设置** | .env 配置编辑（分组可折叠）、单字段保存、批量保存、智能重启 |

#### 关键前端实现

- **Markdown 渲染**: 自定义 `renderMarkdown()` 函数，支持标题/粗体/斜体/代码块/表格/列表/引用
- **表格渲染**: 使用 `\x00` 占位符处理 `<br>` 标签，避免行分割破坏表格结构
- **消息宽度**: AI 消息 `max-width:100%` + `overflow-x:auto`，用户消息 `max-width:80%` 右对齐
- **可折叠区域**: `toggleCollapse(id)` 模式，展开时懒加载数据
- **对话历史**: `chatMessages` 全局状态，每轮对话后自动保存，标题取首条用户消息前 20 字

**对话上下文收集 — sendChat() 关键逻辑**:
```javascript
async function sendChat() {
  // ... 添加用户消息到 chatMessages ...

  // 构建对话历史（最近 10 轮 user/ai 消息）
  const history = [];
  for (const m of chatMessages) {
    if (m.type === 'user') {
      history.push({ role: 'user', content: m.content });
    } else if (m.type === 'ai' && m.content) {
      history.push({ role: 'assistant', content: m.content });
    }
    if (history.length >= 20) break;  // 最多 20 条 (10 轮)
  }
  history.pop();  // 去掉最后一条（刚添加的当前消息）

  // 通过 Tauri invoke 调用 Rust 后端代理 SSE 请求
  const raw = await invoke('chat_request', {
    query: msg,
    history: history.length > 0 ? history : null
  });

  // 解析返回的结构化 JSON（非 SSE 流，Rust 已解析完毕）
  const data = JSON.parse(raw);

  // 处理任务分解可视化、AI 回复、SR 信息、成本摘要
  if (data.decompose) { /* 渲染分解视图 */ }
  if (data.result) {
    chatMessages.push({
      type: 'ai',
      content: data.result.response || '',
      sr_info: data.result.sr_info || '',  // SR 域分类信息
    });
  }

  await saveCurrentConv();  // 自动保存对话
}
```

> **设计要点**: 前端收集最近 10 轮对话历史（20 条消息），通过 `invoke('chat_request')` 传递给 Rust 后端。Rust 后端用 curl 代理 SSE 请求并解析事件流，返回结构化 JSON。前端不需要处理 SSE 流式解析，只需渲染最终结果。

### 8.3 Rust 后端（main.rs）

> **Tauri v2 安全模型**: Tauri v2 的 WebView 运行在安全上下文（`tauri://localhost`）中，内容安全策略（CSP）会阻止从 HTTPS 页面发起 HTTP 请求（混合内容）。这意味着前端 JS 无法直接 `fetch('http://localhost:3002')` 或使用 `EventSource`。解决方案是通过 Rust 后端代理网络请求 [$TRAE_REF](https://v2.tauri.app/security/csp/)。

#### Tauri 命令清单

| 命令 | 功能 |
|------|------|
| `get_services_status` | 查询 Docker 容器状态 |
| `start_services` | 启动所有 Docker 服务 |
| `stop_services` | 停止所有 Docker 服务 |
| `restart_service` | 重启指定服务 |
| `read_env` | 读取 .env 文件 |
| `write_env` | 写入单个 .env 键 |
| `write_env_batch` | 批量写入 .env 键 |
| `get_usage_stats` | 获取用量统计（调用 :3002/api/stats） |
| `get_quota` | 获取配额数据 |
| `get_trends` | 获取趋势数据 |
| `get_service_logs` | 获取服务日志 |
| `update_user_quota` | 更新用户预算 |
| `update_key_quota` | 更新密钥预算 |
| `chat_request` | SSE 聊天代理（解决混合内容拦截） |
| `list_conversations` | 列出对话历史 |
| `load_conversation` | 保存对话 |
| `save_conversation` | 保存对话 |
| `delete_conversation` | 删除对话 |
| `smart_restart` | 智能重启（按变更键映射服务） |
| `get_models` | 获取模型列表（调用 CLI JSON 命令） |
| `add_model` | 添加模型（调用 CLI JSON 命令） |
| `remove_model` | 删除模型（调用 CLI JSON 命令） |
| `open_web_interface` | 打开网页 |
| `run_cli` | 执行 CLI 命令 |
| `check_docker` | 检查 Docker 是否安装 |
| `check_ollama` | 检查 Ollama 是否运行 |

#### 关键实现

**PATH 环境增强**:
```rust
fn enhanced_path() -> String {
    // macOS GUI 应用从 Finder/Dock 启动时 PATH 极其精简
    // 不包含 /usr/local/bin 或 /opt/homebrew/bin
    // 导致找不到 docker/curl/python3 等命令
    let current = std::env::var("PATH").unwrap_or_default();
    let extra = [
        "/usr/local/bin",
        "/opt/homebrew/bin",
        "/opt/homebrew/sbin",
        "/Applications/Docker.app/Contents/Resources/bin",  // Docker CLI
        "/Library/Frameworks/Python.framework/Versions/3.12/bin",  // Python
        // ... 更多路径
    ];
    let mut parts: Vec<&str> = extra.to_vec();
    parts.extend(current.split(':'));  // 额外目录前置，优先查找
    parts.join(":")
}
```

> **踩坑记录**: 不注入 PATH 时，Tauri App 的 Docker 命令全部报 "not found"，但终端中能正常使用。根因是 macOS GUI 应用从 Finder/Dock 启动时继承的是 `/usr/bin:/bin:/usr/sbin:/sbin` 这个极简 PATH，不包含 Homebrew 和 Docker Desktop 的路径。

**命令执行函数 — Python 环境清理**:
```rust
fn cmd(binary: &str) -> Command {
    let mut c = Command::new(binary);
    c.env("PATH", enhanced_path());

    // 关键修复: 清除 TRAE IDE 注入的 Python 环境变量
    // TRAE 会设置 PYTHONHOME 和 PYTHONPATH 指向其内置 Python
    // 这会导致系统 Python (如 Homebrew 的 python3) 崩溃
    // 表现为: python3 litellm_cli.py list-models-json 直接 segfault
    c.env_remove("PYTHONHOME");
    c.env_remove("PYTHONPATH");
    c
}
```

> **踩坑记录**: 从 TRAE IDE 启动 Tauri App 时，TRAE 会向子进程注入 `PYTHONHOME` 和 `PYTHONPATH` 环境变量。当 Tauri App 调用系统 `python3` 执行 `litellm_cli.py` 时，这些变量导致 Python 解释器加载了错误的模块路径，直接崩溃。移除这两个变量后问题解决。

**Docker 命令必须使用 current_dir**:
```rust
// Tauri app 的 docker compose 命令必须指定工作目录
// 否则 docker compose ps 找不到 docker-compose.yml
// App 会卡在 "检查中..." 状态
cmd("docker").args(["compose", "ps"]).current_dir(&dir)
```

**SSE 代理 — chat_request 命令**:
```rust
// Tauri webview (tauri://localhost) 是安全上下文
// 无法直接 EventSource/fetch 到 http://localhost:3002（混合内容拦截）
// 通过 Rust 后端发起 curl 请求，解析 SSE 事件流，返回结构化 JSON
#[tauri::command]
fn chat_request(query: String, history: Option<Vec<HashMap<String, String>>>) -> Result<String, String> {
    // 构建 POST body（包含 query 和 history）
    let body = serde_json::json!({"q": query, "history": history_arr}).to_string();

    // 使用 curl 发起 SSE 请求（--max-time 120 防止无限等待）
    let mut curl = cmd("curl")
        .args(["-sf", "-N", "--max-time", "120", "-X", "POST",
               "-H", "Content-Type: application/json",
               "-d", &body, &url])
        .stdout(Stdio::piped()).stderr(Stdio::piped())
        .spawn()?;

    // 逐行解析 SSE 事件流
    let reader = BufReader::new(curl.stdout.take().unwrap());
    for line in reader.lines() {
        let line = line?;
        if line.starts_with("event: ") {
            current_event = line[7..].to_string();
        } else if line.starts_with("data: ") {
            let data = &line[6..];
            match current_event.as_str() {
                "decompose" => decompose_data = data.to_string(),
                "task_start" => tasks.push(serde_json::from_str(data)?),
                "task_done" => {
                    // 通过 id 匹配更新 task_start 收集的任务对象
                    if let Some(task) = tasks.iter_mut().find(|t| t.id == done_id) {
                        task.result = done_result;
                    }
                }
                "result" => result_data = data.to_string(),
                _ => {}
            }
        }
    }

    // 返回结构化 JSON 给前端
    Ok(serde_json::json!({
        "decompose": decompose_data,
        "tasks": tasks,
        "result": result_data
    }).to_string())
}
```

> **设计原因**: 前端无法直接使用 `new EventSource('http://localhost:3002/...')`，因为 Tauri 的 WebView 运行在安全上下文中。通过 Rust 后端代理 SSE 请求，解析事件流后返回结构化 JSON，前端用 `invoke('chat_request')` 调用。这比使用 `tauri-plugin-http` 更简单，因为不需要修改 CSP 配置 [$TRAE_REF](https://v2.tauri.app/security/csp/)。

**智能重启服务映射**:
```rust
fn services_for_env_keys(keys: &[String]) -> Vec<String> {
    // 根据变更的配置键映射到需要重启的 Docker 服务
    // 避免全量重启，减少停机时间
    let mut worker = false;
    let mut admin = false;
    let mut orch = false;
    let mut webui = false;

    for key in keys {
        match key.as_str() {
            // API Key 变更 → worker + admin 都需要重启（读取 env_file）
            "DASHSCOPE_API_KEY" | "OPENAI_API_KEY" | "ANTHROPIC_API_KEY" | "OR_API_KEY" => {
                worker = true; admin = true;
            }
            // Master Key 变更 → 全量重启（所有服务都使用此 Key）
            "LITELLM_MASTER_KEY" => {
                worker = true; admin = true; orch = true; webui = true;
            }
            // Redis 密码变更 → worker + admin（都连接 Redis）
            "REDIS_PASSWORD" => {
                worker = true; admin = true;
            }
            // Ollama 地址变更 → worker + orchestrator-web
            "OLLAMA_API_BASE" => {
                worker = true; orch = true;
            }
            // Qdrant Key → 仅 worker（只有 worker 使用语义缓存）
            "QDRANT_API_KEY" => {
                worker = true;
            }
            _ => { worker = true; admin = true; }  // 未知 key 兜底全量
        }
    }

    // 使用 --force-recreate --no-deps 重启
    // --force-recreate: 强制重新加载 env_file
    // --no-deps: 不影响依赖服务
    // docker compose up -d --force-recreate --no-deps <services>
}
```

> **重要**: 重启后需要轮询健康检查（最多 60 秒，每 5 秒检查一次），确保服务恢复后再返回。容器名映射需 `replace("litellm-", "litellm_")` 匹配 docker compose ps 输出的容器名格式。

### 8.4 构建与打包

```bash
# 构建脚本
cd tauri-app
./build-tauri.sh

# 或手动构建
cd src-tauri
cargo build --release
# 生成 .app 到 target/release/
```

打包资源（tauri.conf.json `bundle.resources`）包含:
- docker-compose.yml, config_*.yaml, custom_callbacks.py
- task_orchestrator.py, litellm_cli.py
- install.sh, webui-init.sh, quota_setup.sh
- redis.conf, prometheus.yml, .env.example
- webapp/*, grafana/**/*

---

## 9. 网页端管理界面

### 9.1 访问方式

- **编排平台**: http://localhost:3002
- **Admin UI**: http://localhost:4000
- **Open WebUI**: http://localhost:3001
- **Grafana**: http://localhost:3000 (admin/admin)

### 9.2 编排平台页面

| 页面 | URL | 功能 |
|------|-----|------|
| 仪表盘 | / | 服务状态、模型数、缓存命中、累计花费、路由统计表 |
| 智能对话 | /chat | SSE 实时对话、任务分解可视化、成本显示 |
| 配置向导 | /config | Ollama 状态、API Key 管理、路由策略表、定价表 |
| 模型管理 | /models | 已注册模型卡片、定价信息 |

### 9.3 Open WebUI 配置

- 连接地址: `http://semantic-router:8888/v1`（所有请求经过 SR 安全检测）
- API Key: `LITELLM_MASTER_KEY`
- 认证: `WEBUI_AUTH=false`（自动登录 admin@localhost）
- 启动脚本: `webui-init.sh` 在容器启动时注入 OpenAI 配置到 config.json

---

## 10. CLI 命令行工具

### 10.1 命令清单

```bash
# 初始化配置（交互式向导）
python3 litellm_cli.py init

# 模型管理
python3 litellm_cli.py add-model          # 交互式添加
python3 litellm_cli.py list-models         # 列出模型
python3 litellm_cli.py remove-model --name <模型名>

# 非交互式（供 Tauri App 调用）
python3 litellm_cli.py add-model-json '{"model_name":"..."}'
python3 litellm_cli.py remove-model-json --name <模型名>
python3 litellm_cli.py list-models-json

# 运维
python3 litellm_cli.py health              # 健康检查
python3 litellm_cli.py status              # 运行时状态
python3 litellm_cli.py logs <服务名>        # 查看日志
python3 litellm_cli.py orchestrate "任务"  # 编排复杂任务
```

### 10.2 init 命令流程

1. 检查 Docker / Docker Compose
2. 检查 Ollama 运行状态和 qwen2.5:latest 模型
3. 交互式收集 API Key（百炼/OpenAI/Anthropic/OpenRouter）
4. 生成 .env 文件
5. 根据 API Key 状态适配路由策略 (TASK_MODEL_MAP)
6. 启动 Docker Compose 服务
7. 健康检查

### 10.3 add-model 命令流程

1. 输入模型基本信息 (model_name)
2. 选择供应商 (dashscope/openai/anthropic/openrouter/custom)
3. 设置速率限制 (RPM/TPM) 和定价
4. 选择任务路由类型 (simple_qa/general/coding/math_logic/complex_reasoning)
5. 标记是否推理模型（自动启用流式）
6. 配置 Fallback 容灾链
7. 写入文件:
   - config_worker.yaml — 模型条目
   - custom_callbacks.py — TASK_MODEL_MAP / INFERENCE_MODELS
   - quota_setup.sh — models 列表
   - config_worker.yaml — fallbacks 链
8. 可选重启 Worker

---

## 11. 部署与安装指南

### 11.1 前置条件

- macOS (Apple Silicon arm64)
- Docker Desktop（含 Docker Compose）
- Python 3（标准库即可，无需 pip install）
- Ollama（可选，用于本地零成本模型层）

### 11.2 首次安装

```bash
cd /Users/orange/litellm-install

# 方法 1: 一键安装
./install.sh

# 方法 2: 手动安装
cp .env.example .env
# 编辑 .env，填入 DASHSCOPE_API_KEY 等密钥
python3 litellm_cli.py init
```

### 11.3 离线部署

```bash
# 在有网络的机器上导出镜像
./save-images.sh

# 将镜像文件和项目目录拷贝到目标机器

# 导入镜像
./load-images.sh

# 执行离线安装
./offline-install.sh
```

### 11.4 Tauri App 安装

```bash
# 构建
cd tauri-app/src-tauri
cargo build --release

# 或使用构建脚本
cd tauri-app
./build-tauri.sh

# 安装到 /Applications
# 注意: macOS SIP 保护 /Applications，需要手动拖拽或使用 osascript
```

### 11.5 验证安装

```bash
# 健康检查
python3 litellm_cli.py health

# 运行状态
python3 litellm_cli.py status

# 配额验证
./quota_setup.sh
```

---

## 12. 运维与监控

### 12.1 Grafana 仪表盘

| 仪表盘 | URL | UID |
|--------|-----|-----|
| LiteLLM Overview | http://localhost:3000/d/litellm-overview | litellm-overview |
| 配额与智能路由 | http://localhost:3000/d/litellm-quota | litellm-quota |

仪表盘存储在 "LiteLLM" 文件夹下，通过 YAML provisioning 自动加载。

### 12.2 关键 Prometheus 指标

| 指标 | 类型 | 标签 | 用途 |
|------|------|------|------|
| `litellm_task_router_classification_total` | Counter | task_type, method, target_model | 路由分类统计 |
| `litellm_quota_key_spend_total` | Counter | key_alias, user_id, model | Key 维度花费 |
| `litellm_quota_user_spend_total` | Counter | user_id, model | User 维度花费 |
| `litellm_quota_key_requests_total` | Counter | key_alias, user_id | Key 维度请求数 |
| `litellm_quota_key_spend_by_model` | Counter | key_alias, model | Key+Model 花费 |
| `litellm_cache_hits_metric_total` | Counter | - | 缓存命中次数 |

### 12.3 常用 PromQL

```promql
# 某个 Key 的总花费
sum(litellm_quota_key_spend_total{key_alias="my-key"}) by (key_alias)

# 按 Model 的花费分布
sum(litellm_quota_key_spend_by_model_total) by (model)

# 路由分类统计
sum(litellm_task_router_classification_total) by (task_type)

# 缓存命中率
litellm_cache_hits_metric_total / ignoring() (litellm_cache_hits_metric_total + total_requests)
```

### 12.4 日常运维命令

```bash
# 查看服务状态
docker compose ps

# 查看某服务日志
python3 litellm_cli.py logs litellm-worker --lines 100

# 重启单个服务
docker compose restart litellm-worker

# 查看运行时状态（路由统计、花费、模型健康）
python3 litellm_cli.py status

# 添加新模型
python3 litellm_cli.py add-model

# 健康检查
python3 litellm_cli.py health
```

---

## 13. 常见问题与排障

### 13.1 语义缓存 401 阻断

**现象**: DASHSCOPE_API_KEY 为空时，所有请求返回 401 错误  
**原因**: Qdrant 语义缓存需要 DashScope embedding 模型，API Key 为空时 embedding 调用 401  
**解决**: 在 .env 中填入 DASHSCOPE_API_KEY，或通过 CLI init 命令自动适配

### 13.2 Redis NOAUTH

**现象**: Worker/Admin 启动后无法连接 Redis  
**原因**: redis.conf 设置了 requirepass，但 REDIS_URL 中未包含密码  
**解决**: 确保 .env 中 `REDIS_URL=redis://:litellm_redis_2026@redis:6379`

### 13.3 推理模型 HTTP 超时

**现象**: 调用 qwen3.6-flash/plus、deepseek-v3 时请求超时  
**原因**: 推理模型生成 reasoning tokens 耗时长，非流式响应在 HTTP 超时前无法返回  
**解决**: task_router 回调自动为推理模型启用 `stream=true`。编排引擎使用 `_call_llm_stream()`

### 13.4 Tauri App 找不到 docker/python3

**现象**: Tauri App 中 Docker 命令或 Python 命令报 "not found"  
**原因**: macOS GUI 应用从 Finder/Dock 启动时继承的 PATH 极其精简  
**解决**: `cmd()` 函数已注入 `enhanced_path()`，包含 `/usr/local/bin`、`/opt/homebrew/bin` 等常见路径

### 13.5 Tauri App Python 崩溃

**现象**: Tauri App 调用 python3 litellm_cli.py 时 Python 进程崩溃  
**原因**: TRAE 环境注入的 PYTHONHOME/PYTHONPATH 与系统 Python 冲突  
**解决**: `cmd()` 函数已 `env_remove("PYTHONHOME")` 和 `env_remove("PYTHONPATH")`

### 13.6 对话历史切换页面后清空

**现象**: 在 Tauri App 对话页面切换到其他页面再回来，聊天记录消失  
**原因**: 前端 chatMessages 状态在页面切换时重置  
**解决**: 已实现对话历史 JSON 持久化（conversations/目录），切换页面后自动加载

### 13.7 Markdown 渲染异常

**现象**: AI 回复中的 # * 等符号原样显示，表格只渲染一行  
**原因**: 
- escapeHtml 转义了所有 Markdown 语法字符
- 模型输出省略了表格行的尾部管道符
- `<br>` 标签在行分割时破坏了表格结构

**解决**: 
- AI 消息使用 `renderMarkdown()` 替代 `escapeHtml()`
- 表格行补全尾部管道: `const normalized = trimmed.endsWith('|') ? trimmed : trimmed + '|';`
- `<br>` 用 `\x00` 占位符替换，行分割后恢复

### 13.8 Open WebUI 没有 auto 选项

**现象**: Open WebUI 模型选择器中看不到 "auto"  
**原因**: config_worker.yaml 中缺少 auto 虚拟模型条目  
**解决**: 确保 model_list 中包含:
```yaml
- model_name: auto
  litellm_params:
    model: openai/qwen-plus
    api_base: os.environ/DASHSCOPE_API_BASE
    api_key: os.environ/DASHSCOPE_API_KEY
```

### 13.9 用量数据被清空

**现象**: Worker 重启后 Prometheus 指标归零  
**原因**: Prometheus Counter 是进程内计数器，Worker 重启后重置  
**解决**: `/api/stats` 端点优先从 LiteLLM Admin 数据库查询累计花费，仅当数据库查询失败时回退到 Prometheus

### 13.10 路由统计不更新

**现象**: 聊天后路由统计数字不变  
**原因**: 路由统计 (`litellm_task_router_classification_total`) 仅在 `model="auto"` 时计数。编排引擎直接选择具体模型执行子任务，不经过 auto 路由  
**解决**: 这是预期行为。只有 `decompose()` 使用 `model="auto"`。简单聊天通过编排引擎直接路由，不更新统计

### 13.11 YAML 语法错误导致 Worker 循环重启

**现象**: Worker 容器反复重启  
**原因**: config_worker.yaml 中 fallbacks 使用了无效 YAML 语法（如多余逗号 `[, "item"]`）  
**解决**: 验证 YAML 语法:
```bash
python3 -c "import yaml; yaml.safe_load(open('config_worker.yaml'))"
```
fallbacks 正确格式: `{ "model_name": ["fallback_model"] }`

### 13.12 PII 正则中文失效

**现象**: 包含中文的文本中 PII 检测不生效  
**原因**: `\b` word boundary 不支持中文字符  
**解决**: 使用 `(?<!\d)` 和 `(?!\d)` lookbehind/lookahead 断言替代 `\b`

---

## 14. 开发约定与注意事项

### 14.1 硬约束

1. **部署目录**: 必须为 `/Users/orange/litellm-install`
2. **控制面/数据面分离**: Admin 和 Worker 必须是两个独立服务
3. **Redis 是硬依赖**: 用于缓存、路由状态、限流
4. **API Key 存储**: 只存在 `.env` 环境变量中，不写入配置文件
5. **模型名一致性**: TASK_MODEL_MAP 键、fallbacks、quota_setup.sh 中的模型名必须与 config_worker.yaml model_list 中的 model_name 完全一致

### 14.2 工程约定

- **配置文件**: 从原始 config.yaml 分离为 config_admin.yaml 和 config_worker.yaml
- **Docker 镜像**: 使用 arm64 原生镜像（华为云镜像源）
- **自定义回调**: 必须是单个 .py 文件（不是包目录），实例名与配置中一致
- **Ollama 集成**: 通过 `host.docker.internal:11434` 访问，Linux 需要 `extra_hosts`
- **SSE 响应**: 必须指定 `charset=utf-8` 防止中文乱码
- **Grafana 仪表盘**: 通过 YAML provisioning 自动加载，存储在 grafana/dashboards 目录
- **Tauri Docker 命令**: 必须使用 `.current_dir(get_install_dir())`
- **Tauri PATH**: 必须通过 `enhanced_path()` 注入常见二进制路径

### 14.3 成本优化策略

1. **简单问答路由到本地模型**: simple_qa → qwen2.5-local（零 API 费用）
2. **规则分类优先**: 正则匹配零成本，LLM 分类仅作兜底
3. **选择性缓存**: 仅 simple_qa/general 类型启用缓存，复杂任务禁用
4. **语义缓存 TTL**: 24 小时自动过期，避免过期数据命中
5. **三层 Fallback**: 云端高质量 → 云端降级 → 本地零成本兜底

### 14.4 安全约定

1. **Redis**: 密码认证 + 危险命令禁用 + protected-mode
2. **PII 检测**: 默认 mask 动作，可配置 block
3. **越狱检测**: 默认 block 动作，拦截 5 种攻击类型
4. **预算上限**: 防止误操作创建超高预算密钥 (max $1000/365d)
5. **Master Key**: 生产环境必须修改默认值 `sk-1234`

### 14.5 已知限制

- LiteLLM 成本追踪是本地估算（config 单价 × token 数），非实时账单
- DB 中存储的自定义模型无成本配置时显示 $0 花费
- 语义缓存依赖 DASHSCOPE_API_KEY，为空时必须禁用缓存
- Qdrant 官方镜像（Debian 13）不含 wget/curl，健康检查使用 `bash -c 'echo > /dev/tcp/localhost/6333'`
- Tauri webview 安全上下文阻止 http:// SSE 连接，必须通过 Rust 后端代理
- macOS SIP 保护 /Applications，无法直接 rm/cp 安装应用

---

## 15. 技术参考与外部文档

> 本节汇总项目中使用的关键技术的官方文档参考，方便开发者深入理解实现原理。

### 15.1 LiteLLM 自定义回调 (Custom Callbacks)

LiteLLM 通过 `CustomLogger` 基类提供生命周期钩子。`async_pre_call_hook` 在请求发送给 LLM 前执行，可修改请求数据（如替换 model）；`async_log_success_event` 在调用成功后异步触发，用于记录日志和指标。回调通过 config.yaml 的 `litellm_settings.callbacks` 注册。

**项目中的应用**: `custom_callbacks.py` 中的 `TaskRouterHandler` 使用 `async_pre_call_hook` 拦截 `model="auto"` 请求，执行智能分类并替换为目标模型；`QuotaTrackerHandler` 使用 `async_log_success_event` 记录花费和请求计数。

**注意事项**: `async_pre_call_hook` 仅在 Proxy 模式下生效；回调中不要执行阻塞操作；异常应被捕获处理，避免中断主流程。

- 文档: https://docs.litellm.ai/docs/observability/custom_callback

### 15.2 LiteLLM Qdrant 语义缓存 (Semantic Cache)

LiteLLM 的 Qdrant 语义缓存通过向量相似度匹配历史 LLM 响应。请求到达时，将输入文本通过 embedding 模型转为向量，在 Qdrant 集合中搜索相似向量；若相似度超过 `similarity_threshold` 阈值则返回缓存响应（cache hit），否则正常调用 LLM 并存入 Qdrant（cache miss）。

**项目中的配置**:
- `qdrant_semantic_cache_embedding_model: bailian-embedding` — 必须在 model_list 中定义
- `similarity_threshold: 0.95` — 高阈值避免误命中
- `ttl: 86400` — 24 小时自动过期
- `qdrant_semantic_cache_vector_size: 1024` — 必须与 embedding 模型维度一致
- `qdrant_quantization_config: binary` — 二值量化节省存储

**注意事项**: embedding 模型指向的条目必须在 `model_list` 中定义；`qdrant_semantic_cache_vector_size` 必须与 embedding 模型输出维度严格一致；凭证通过 `QDRANT_API_KEY` 和 `QDRANT_API_BASE` 环境变量配置。

- 文档: https://docs.litellm.ai/docs/proxy/caching

### 15.3 LiteLLM Fallbacks 与路由策略

LiteLLM 的 Fallback 机制在调用耗尽 `num_retries` 重试次数后仍失败时，按顺序回退到备用模型组。存在三种回退类型: `fallbacks`（常规错误）、`context_window_fallbacks`（上下文超限）、`content_policy_fallbacks`（内容策略违规）。

`usage-based-routing-v2` 是异步实现版本，过滤已超出 TPM/RPM 限制的部署，路由到当前使用量最低的部署。生产环境使用 Redis 跟踪跨部署的使用量。

**项目中的配置**:
```yaml
router_settings:
  routing_strategy: usage-based-routing-v2
  fallbacks:
    - { "qwen3.6-plus": ["qwen-plus"] }
    - { "qwen3-max": ["qwen3.6-plus"] }
    # ...
```

**注意事项**: `enable_pre_call_checks: true` 是上下文窗口强制的必要条件；官方文档指出 `usage-based-routing` 系列因 Redis 操作延迟，不推荐用于高并发生产环境，可考虑 `simple-shuffle`。

- Fallbacks 文档: https://docs.litellm.ai/docs/proxy/reliability
- 路由策略文档: https://docs.litellm.ai/docs/routing

### 15.4 Tauri v2 安全模型 (CSP 与混合内容)

Tauri v2 通过内容安全策略（CSP）限制 WebView 中可加载的资源。由于 WebView 运行在 `https://tauri.localhost` 安全上下文中，直接从前端发起 `http://` 请求构成混合内容（mixed content），被安全策略阻止。

**项目中的解决方案**: 不使用 `tauri-plugin-http` 插件，而是通过 Rust 后端 `chat_request` 命令代理 SSE 请求。Rust 端使用 curl 发起 HTTP 请求，解析 SSE 事件流后返回结构化 JSON 给前端。这避免了修改 CSP 配置的复杂性。

**注意事项**: 避免加载 CDN 远程脚本（引入攻击向量）；如使用 WebAssembly 需在 `script-src` 中包含 `'wasm-unsafe-eval'`；`connect-src` 中的 `ipc: http://ipc.localhost` 是 Tauri IPC 通信所必需。

- CSP 文档: https://v2.tauri.app/security/csp/
- Capabilities 文档: https://v2.tauri.app/security/capabilities/
- HTTP 插件: https://github.com/tauri-apps/tauri-plugin-http

### 15.5 Qdrant 向量相似度搜索

Qdrant 支持的距离度量包括 Cosine（余弦相似度，最常用）、Dot（点积）、Euclidean（欧几里得距离）。对于 Cosine 度量，Qdrant 在向量插入时归一化（仅一次），搜索时等同于点积运算，利用 SIMD 指令实现极快速度。

**Binary Quantization（二值量化）** 将每个向量分量压缩为 1 个 bit，实现 32 倍压缩，是最快的量化方法。建议配合 rescoring 使用以提升搜索质量。对于 1024 维向量，binary quantization 配合 oversampling 可达到高召回率。

**项目中的应用**: `qdrant_quantization_config: binary` 配置二值量化，`similarity_threshold: 0.95` 是 LiteLLM 侧的过滤参数（非 Qdrant 原生参数），用于决定是否将缓存结果视为命中。

- 相似度搜索: https://qdrant.tech/documentation/concepts/search/
- 量化配置: https://qdrant.tech/documentation/manage-data/quantization/

### 15.6 对话上下文数据流

项目中对话历史（context）的完整传递路径:

```
Tauri App 前端 (index.html)
  │  sendChat() 构建 history 数组（最近 10 轮 user/ai 消息）
  │  history.push({role: "user", content: ...})
  │  history.push({role: "assistant", content: ...})
  │
  ▼  invoke('chat_request', { query, history })
Tauri Rust 后端 (main.rs)
  │  chat_request() 接收 history: Option<Vec<HashMap<String, String>>>
  │  构建 POST body: {"q": query, "history": [...]}
  │  curl POST → orchestrator-web :3002/api/chat/stream
  │
  ▼
Orchestrator Web (webapp/app.py)
  │  chat_stream() 接收 data.get("history", [])
  │  传递给 orch.orchestrate(query, history=history)
  │
  ▼
Task Orchestrator (task_orchestrator.py)
  │  orchestrate(query, history)
  │  ├─ execute_task(task, context, history)
  │  │   └─ messages.extend(history[-10:])  # 注入最近 10 轮
  │  └─ aggregate(query, sub_tasks, history)
  │      └─ messages.extend(history[-6:])   # 聚合时窗口更小
  │
  ▼
LiteLLM Worker (:4001)
  │  /v1/chat/completions
  │  messages 数组包含完整的对话历史 + 当前问题
  │
  ▼
模型供应商 (DashScope / Ollama)
     LLM 基于完整上下文生成连贯回答
```

> **设计要点**: `execute_task` 保留最近 10 轮历史确保子任务执行时有足够上下文；`aggregate` 只保留 6 轮以控制 token 消耗（汇总不需要太多历史）。两者都使用负索引切片（`history[-N:]`）自动丢弃过旧的历史。

---

## 附录 A: 模型定价表

| 模型 | 输入 ($/token) | 输出 ($/token) | 说明 |
|------|---------------|---------------|------|
| qwen2.5-local | 0 | 0 | Ollama 本地（零成本） |
| qwen3.6-flash | 0.00000167 | 0.00001 | 百炼 Flash（极速） |
| qwen-plus | 0.00000111 | 0.00000278 | 百炼 Plus（性价比） |
| deepseek-v3 | 0.00000139 | 0.00001111 | DeepSeek V3（推理） |
| qwen3.6-plus | 0.00000278 | 0.00001667 | 百炼 Plus+（高质量） |
| qwen3-max | 0.00000347 | 0.00001389 | 百炼 Max（旗舰） |
| gpt-4o | 0.0000025 | 0.00001 | OpenAI |
| claude-3-5-sonnet | 0.000003 | 0.000015 | Anthropic |

> 定价来源：阿里云百炼官方定价页（中国内地，2026-05 更新），汇率 1 USD ≈ 7.2 CNY

## 附录 B: 端口清单

| 端口 | 服务 | 用途 |
|------|------|------|
| 3000 | Grafana | 可视化面板 |
| 3001 | Open WebUI | 聊天前端 |
| 3002 | Orchestrator Web | 编排平台 + 管理 API |
| 4000 | LiteLLM Admin | 控制面 UI + API |
| 4001 | LiteLLM Worker | 数据面推理 API |
| 5432 | PostgreSQL | 数据库 |
| 6333 | Qdrant (REST) | 语义缓存向量库 |
| 6334 | Qdrant (gRPC) | 语义缓存向量库 |
| 6379 | Redis | 路由状态 + 限流 |
| 8888 | Semantic Router | 安全检测代理 |
| 9090 | Prometheus | 指标采集 |
| 11434 | Ollama | 本地模型推理 |

## 附录 C: 典型复杂问题示例（触发 auto 路由多模型协作）

1. **开发+测试**: "用 Python 写一个 Web 爬虫并测试它"
2. **对比分析**: "对比 React 和 Vue 的优缺点并给出选择建议"
3. **多步处理**: "翻译这段英文并分析其中的修辞手法"
4. **数学+代码**: "解释快速排序的原理并用 Python 实现"
5. **研究+方案**: "分析微服务架构的优缺点并设计一个迁移方案"
